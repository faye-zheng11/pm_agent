# 网关凭据模板 —— 复制本文件为 internal-gateway.key 再填入
#
#   cp bootstrap/internal-gateway.key.example bootstrap/internal-gateway.key
#
# 说明：
# - 如果你本机已登录 Codex（~/.codex/auth.json 里有 OPENAI_API_KEY），
#   setup.command 会优先用它，这个文件可以不填。
# - 否则把公司共享网关的 API Key 粘到下面这行（替换掉占位符），保存后运行 setup.command。
#   setup 会把它导入 macOS Keychain；导入后可以删掉这个 .key 文件。
# - internal-gateway.key 已被 .gitignore 忽略；无论如何都不要把真实 Key 提交回仓库。

sk-REPLACE_WITH_YOUR_GATEWAY_API_KEY
